function solution(s) {
    // const arr = s.split('');
    // let cnt = 1;
    // for (let i = arr.length - 1; i >= 0; i--) {
    //     if (i === 0 && arr[i] == '-') {
    //         answer -= answer * 2;
    //         return answer;
    //     }
    //     answer += arr[i] * cnt;
    //     cnt = cnt * 10;
    // }
    return Number(s);
}